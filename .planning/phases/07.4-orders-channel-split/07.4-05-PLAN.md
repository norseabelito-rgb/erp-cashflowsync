---
phase: 07.4-orders-channel-split
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/orders/temu-placeholder.tsx
  - src/app/(dashboard)/orders/page.tsx
autonomous: true

must_haves:
  truths:
    - "Temu tab shows placeholder message 'Urmeaza sa fie implementat'"
    - "Placeholder has construction/coming-soon icon"
    - "No order table or filters shown for Temu tab"
    - "Message is in Romanian without diacritics"
  artifacts:
    - path: "src/components/orders/temu-placeholder.tsx"
      provides: "Placeholder component for Temu tab"
      exports: ["TemuPlaceholder"]
    - path: "src/app/(dashboard)/orders/page.tsx"
      provides: "Orders page with Temu placeholder integration"
      contains: "TemuPlaceholder"
  key_links:
    - from: "src/app/(dashboard)/orders/page.tsx"
      to: "activeTab === 'temu'"
      via: "conditional rendering"
      pattern: "activeTab.*temu.*TemuPlaceholder"
---

<objective>
Create a placeholder component for the Temu tab showing "Coming soon" message.

Purpose: Prepare the UI structure for future Temu integration while clearly indicating it's not yet available.

Output: TemuPlaceholder component with appropriate messaging and icon.
</objective>

<execution_context>
@/Users/stefanpanaite/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefanpanaite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.4-orders-channel-split/07.4-CONTEXT.md

@src/app/(dashboard)/orders/page.tsx
@src/components/ui/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TemuPlaceholder component</name>
  <files>src/components/orders/temu-placeholder.tsx</files>
  <action>
Create a simple placeholder component for the Temu tab:

```typescript
"use client";

import { Construction } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";

export function TemuPlaceholder() {
  return (
    <Card className="mt-4">
      <CardContent className="flex flex-col items-center justify-center py-16 text-center">
        <div className="rounded-full bg-muted p-4 mb-4">
          <Construction className="h-12 w-12 text-muted-foreground" />
        </div>
        <h3 className="text-lg font-semibold mb-2">
          Integrare Temu
        </h3>
        <p className="text-muted-foreground max-w-md">
          Urmeaza sa fie implementat. Comenzile din Temu vor fi sincronizate automat dupa finalizarea integrarii.
        </p>
        <p className="text-sm text-muted-foreground mt-4">
          Aceasta functionalitate este planificata pentru o versiune viitoare.
        </p>
      </CardContent>
    </Card>
  );
}
```

Key elements:
- Construction icon (from lucide-react) in muted circle
- Title: "Integrare Temu"
- Primary message: "Urmeaza sa fie implementat..."
- Secondary message: "Aceasta functionalitate este planificata..."
- Centered layout with appropriate padding
- No diacritics in Romanian text
  </action>
  <verify>
File exists at src/components/orders/temu-placeholder.tsx with:
- TemuPlaceholder export
- Construction icon import
- Romanian text without diacritics
  </verify>
  <done>
TemuPlaceholder component displays clear "coming soon" message with construction icon.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate TemuPlaceholder into orders page</name>
  <files>src/app/(dashboard)/orders/page.tsx</files>
  <action>
Update the orders page to show the placeholder when Temu tab is active:

1. Import the component:
```typescript
import { TemuPlaceholder } from "@/components/orders/temu-placeholder";
```

2. Modify the tab content rendering:
   - When activeTab === "temu", show TemuPlaceholder instead of order table
   - Hide filters, selection bar, and pagination for Temu tab
   - Keep the PageHeader and ChannelTabs visible

3. Update the render logic (after ChannelTabs, before filters):
```tsx
{activeTab === "temu" ? (
  <TemuPlaceholder />
) : (
  <>
    {/* Existing filter card */}
    <Card className="mb-4 md:mb-6">
      ...
    </Card>

    {/* Selection bar */}
    {selectedOrders.length > 0 && (...)}

    {/* Error panel */}
    {processErrors.length > 0 && (...)}

    {/* Orders table */}
    <Tabs value={activeTab} ...>
      ...
    </Tabs>

    {/* Pagination */}
    ...
  </>
)}
```

4. Ensure ChannelTabs count for Temu is always 0 (already handled in Plan 01)

5. Optional: Add subtle styling to Temu tab trigger to indicate it's a placeholder:
   - Could add opacity-70 or different badge styling
   - This is already partially done via "neutral" badge variant
  </action>
  <verify>
1. Navigate to /orders
2. Click Temu tab
3. Placeholder message shows instead of table
4. Filters hidden
5. Selection bar hidden
6. Click back to Shopify - normal view returns
  </verify>
  <done>
Orders page shows TemuPlaceholder when Temu tab is active, hiding filters and order table.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` - No TypeScript errors
2. Manual test: Click Temu tab, verify placeholder appears
3. Manual test: Verify filters and table are hidden
4. Manual test: Switch back to Shopify tab, verify normal view
5. Verify: Message is in Romanian without diacritics
</verification>

<success_criteria>
- Temu tab shows placeholder component
- Message "Urmeaza sa fie implementat" visible
- Construction/coming-soon icon displayed
- Order table and filters hidden when Temu active
- Switching tabs works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07.4-orders-channel-split/07.4-05-SUMMARY.md`
</output>
