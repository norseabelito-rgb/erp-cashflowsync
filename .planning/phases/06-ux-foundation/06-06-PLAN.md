---
phase: 06-ux-foundation
plan: 06
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(dashboard)/orders/page.tsx
  - src/app/(dashboard)/invoices/page.tsx
  - src/app/(dashboard)/products/page.tsx
  - src/app/(dashboard)/inventory/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Every action button shows tooltip on hover"
    - "Tooltips describe action and consequence in Romanian"
    - "Disabled buttons show reason why disabled"
  artifacts:
    - path: "src/app/(dashboard)/orders/page.tsx"
      provides: "ActionTooltip on invoice, AWB, view, edit buttons"
      contains: "ActionTooltip"
    - path: "src/app/(dashboard)/invoices/page.tsx"
      provides: "ActionTooltip on action dropdown items"
      contains: "ActionTooltip"
    - path: "src/app/(dashboard)/products/page.tsx"
      provides: "ActionTooltip on sync, create, edit buttons"
      contains: "ActionTooltip"
    - path: "src/app/(dashboard)/inventory/page.tsx"
      provides: "ActionTooltip on add, import, action buttons"
      contains: "ActionTooltip"
  key_links:
    - from: "orders/page.tsx"
      to: "@/components/ui/action-tooltip"
      via: "import"
      pattern: "import.*ActionTooltip.*from.*action-tooltip"
    - from: "invoices/page.tsx"
      to: "@/components/ui/action-tooltip"
      via: "import"
      pattern: "import.*ActionTooltip.*from.*action-tooltip"
    - from: "products/page.tsx"
      to: "@/components/ui/action-tooltip"
      via: "import"
      pattern: "import.*ActionTooltip.*from.*action-tooltip"
    - from: "inventory/page.tsx"
      to: "@/components/ui/action-tooltip"
      via: "import"
      pattern: "import.*ActionTooltip.*from.*action-tooltip"
---

<objective>
Apply ActionTooltip component to all action buttons across dashboard pages.

Purpose: Close verification gap - ActionTooltip exists (06-01) but is orphaned with 0 imports. Success criterion 1 ("Every button and action has a descriptive tooltip explaining what it does") is currently FAILED.

Output: All action buttons wrapped with ActionTooltip showing Romanian action + consequence descriptions.
</objective>

<execution_context>
@/Users/stefanpanaite/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefanpanaite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-foundation/06-01-SUMMARY.md

Reference the ActionTooltip component created in 06-01:
@src/components/ui/action-tooltip.tsx

Pages to update:
@src/app/(dashboard)/orders/page.tsx
@src/app/(dashboard)/invoices/page.tsx
@src/app/(dashboard)/products/page.tsx
@src/app/(dashboard)/inventory/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ActionTooltip to Orders page buttons</name>
  <files>src/app/(dashboard)/orders/page.tsx</files>
  <action>
    Import ActionTooltip from "@/components/ui/action-tooltip".

    Wrap the following action buttons with ActionTooltip:

    1. **Generate Invoice button** (icon-only, in table row ~line 1310):
       - action: "Genereaza factura"
       - consequence: "Se trimite catre Oblio"
       - disabled: when invoiceMutation.isPending
       - disabledReason: "Se proceseaza..."

    2. **Generate AWB button** (icon-only, in table row ~line 1351):
       - action: "Creeaza AWB"
       - consequence: "Se genereaza AWB in SelfAWB"
       - disabled: when awbMutation.isPending
       - disabledReason: "Se proceseaza..."

    3. **View Order button** (icon-only, in actions column ~line 1356):
       - action: "Vezi detalii comanda"
       - No consequence (just viewing)

    4. **Edit Order button** (icon-only, in actions column ~line 1358):
       - action: "Editeaza comanda"
       - consequence: "Modificarile se salveaza"

    5. **Bulk action buttons** (in selection bar ~lines 1119-1140):
       - "Emite Facturi": action="Emite facturi pentru comenzile selectate", consequence="Se trimit catre Oblio"
       - "Creaza AWB": action="Creeaza AWB-uri pentru comenzile selectate", consequence="Se genereaza in SelfAWB"
       - "Proceseaza tot": action="Proceseaza complet comenzile selectate", consequence="Factura + AWB pentru fiecare"

    6. **Sync button** (header ~line 1020):
       - action: "Sincronizeaza comenzi"
       - consequence: "Se importa comenzile noi din Shopify"
       - disabled: when syncMutation.isPending
       - disabledReason: "Sincronizare in curs..."

    Pattern: Wrap Button with ActionTooltip, keeping Button as child:
    ```tsx
    <ActionTooltip
      action="Genereaza factura"
      consequence="Se trimite catre Oblio"
      disabled={invoiceMutation.isPending}
      disabledReason="Se proceseaza..."
    >
      <Button size="sm" variant="ghost" ...>
        <FileText className="h-4 w-4" />
      </Button>
    </ActionTooltip>
    ```
  </action>
  <verify>
    1. `grep -n "ActionTooltip" src/app/(dashboard)/orders/page.tsx` shows 6+ occurrences
    2. `grep -n "import.*ActionTooltip" src/app/(dashboard)/orders/page.tsx` shows import
    3. TypeScript: `npx tsc --noEmit` passes
  </verify>
  <done>
    Orders page buttons show Romanian tooltips on hover with action + consequence format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply ActionTooltip to Invoices page buttons</name>
  <files>src/app/(dashboard)/invoices/page.tsx</files>
  <action>
    Import ActionTooltip from "@/components/ui/action-tooltip".

    Wrap the following action buttons with ActionTooltip:

    1. **Refresh button** (header ~line 315):
       - action: "Reincarca facturi"
       - consequence: "Se actualizeaza lista"

    2. **Help button** (header ~line 311):
       - action: "Afiseaza ajutor"
       - No consequence

    3. **Actions dropdown trigger** (in table rows, MoreHorizontal icon ~line 566):
       - action: "Actiuni"
       - consequence: "Vezi optiunile disponibile"

    The dropdown menu items (View, Download, Cancel, Mark Paid) are DropdownMenuItem, not Button.
    ActionTooltip works best with Button children, so only wrap actual Button components.

    4. **Cancel dialog buttons** (~lines 692-698):
       - Cancel button: no tooltip needed (dialog action)
       - Confirm cancel button: action="Confirma anularea", consequence="Factura va fi stornata in Oblio"

    5. **Pay dialog buttons** (~lines 927-933):
       - Cancel button: no tooltip needed
       - Confirm pay button: action="Marcheaza ca platita", consequence="Se actualizeaza statusul platii"

    Pattern: Same as Orders page - wrap Button with ActionTooltip keeping Button as child.
  </action>
  <verify>
    1. `grep -n "ActionTooltip" src/app/(dashboard)/invoices/page.tsx` shows 4+ occurrences
    2. `grep -n "import.*ActionTooltip" src/app/(dashboard)/invoices/page.tsx` shows import
    3. TypeScript: `npx tsc --noEmit` passes
  </verify>
  <done>
    Invoices page action buttons show Romanian tooltips on hover.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply ActionTooltip to Products and Inventory pages</name>
  <files>
    src/app/(dashboard)/products/page.tsx
    src/app/(dashboard)/inventory/page.tsx
  </files>
  <action>
    Import ActionTooltip in both files from "@/components/ui/action-tooltip".

    **Products page:**

    1. **Create Product button** (~line 581):
       - action: "Adauga produs nou"
       - consequence: "Se deschide formularul de creare"

    2. **Import buttons** (~lines 558, 565):
       - CSV import: action="Importa produse din CSV", consequence="Se incarca produse in bulk"
       - Shopify sync: action="Sincronizeaza din Shopify", consequence="Se actualizeaza produsele existente"

    3. **Row action button** (edit icon ~line 838):
       - action: "Editeaza produsul"
       - consequence: "Modificarile se salveaza"

    4. **Selection action buttons** (~lines 878, 886):
       - Deselect: action="Deselecteaza tot"
       - Export: action="Exporta selectia", consequence="Se descarca fisier CSV"

    **Inventory page:**

    1. **Refresh button** (~line 376):
       - action: "Reincarca inventar"
       - consequence: "Se actualizeaza lista"

    2. **Import button** (~line 382):
       - action: "Importa din CSV"
       - consequence: "Se actualizeaza stocurile in bulk"

    3. **Add new item button** (~line 405):
       - action: "Adauga articol nou"
       - consequence: "Se deschide formularul de creare"

    4. **Row action button** (edit icon ~line 635):
       - action: "Editeaza articolul"
       - consequence: "Modificarile se salveaza"

    Pattern: Same wrapping approach as previous tasks.
  </action>
  <verify>
    1. `grep -n "ActionTooltip" src/app/(dashboard)/products/page.tsx` shows 5+ occurrences
    2. `grep -n "ActionTooltip" src/app/(dashboard)/inventory/page.tsx` shows 4+ occurrences
    3. `grep -n "import.*ActionTooltip" src/app/(dashboard)/products/page.tsx` shows import
    4. `grep -n "import.*ActionTooltip" src/app/(dashboard)/inventory/page.tsx` shows import
    5. TypeScript: `npx tsc --noEmit` passes
  </verify>
  <done>
    Products and Inventory pages have ActionTooltip on all major action buttons.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Import verification:**
   ```bash
   grep -l "ActionTooltip" src/app/\(dashboard\)/**/page.tsx
   ```
   Should list: orders, invoices, products, inventory pages.

2. **Component no longer orphaned:**
   ```bash
   grep -r "import.*ActionTooltip" src/app/
   ```
   Should show 4 imports (one per page).

3. **TypeScript compilation:**
   ```bash
   npx tsc --noEmit
   ```
   No errors.

4. **Runtime verification:**
   Start dev server, hover over buttons - tooltips should appear after 300ms with Romanian text.
</verification>

<success_criteria>
- ActionTooltip imported in 4 dashboard pages
- 15+ buttons wrapped with ActionTooltip across all pages
- All tooltips use Romanian text with action/consequence format
- Disabled buttons show disabledReason when applicable
- TypeScript compiles without errors
- Verification gap "Every button and action has a descriptive tooltip" is now PASSED
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-foundation/06-06-SUMMARY.md`
</output>
